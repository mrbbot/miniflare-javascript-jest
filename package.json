{
  "name": "miniflare-javascript-jest",
  "description": "Example project using Miniflare, JavaScript and Jest",
  "version": "1.0.0",
  "module": "src/index.js",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "dev": "wrangler dev --local",
    "dev:remote": "wrangler dev",
    "test": "node --experimental-vm-modules --no-warnings node_modules/jest/bin/jest.js",
    "deploy": "wrangler publish"
  },
  "devDependencies": {
    "jest": "^29.0.3",
    "jest-environment-miniflare": "^2.8.2",
    "wrangler": "^2.1.3"
  }
}
